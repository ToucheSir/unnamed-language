void main() {
    int a;
    int b;
    boolean c;
    boolean[3] d;
    a = 1;
    b = a - 4;
    a = a * b + 5 - 21 * b * b * a;
    c = a == b + 5 * 1 * (a + 1) - 3 < 5;
    d[1] = a < c == b + func() - 5 * (func(d[2])) + d[3] == (d[0]) == false + ('a' - 'b');
    println "foo " + "bar \n";
    (a*n()*(s==((('4')))+'2'));
}